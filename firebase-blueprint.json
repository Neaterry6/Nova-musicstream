{
  "entities": {
    "User": {
      "title": "User Profile",
      "description": "User account information",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "displayName": { "type": "string" },
        "email": { "type": "string" },
        "photoURL": { "type": "string" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "themePreference": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "email"]
    },
    "SongRequest": {
      "title": "Song Request",
      "description": "A user's request for a song",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "songName": { "type": "string" },
        "artist": { "type": "string" },
        "requesterEmail": { "type": "string" },
        "requesterId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "processed", "rejected"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["songName", "requesterId"]
    },
    "Playlist": {
      "title": "Playlist",
      "description": "User created music playlist",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "ownerId": { "type": "string" },
        "tracks": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "title": { "type": "string" },
              "artist": { "type": "string" },
              "cover": { "type": "string" },
              "url": { "type": "string" }
            }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "name", "ownerId"]
    },
    "Admin": {
      "title": "Administrator",
      "description": "Users with admin privileges",
      "type": "object",
      "properties": {
        "email": { "type": "string" },
        "addedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["email"]
    },
    "TrendingTrack": {
      "title": "Trending Track",
      "description": "Track fetched from trending lists",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "artist": { "type": "string" },
        "cover": { "type": "string" },
        "url": { "type": "string" },
        "category": { "type": "string" }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "Public and private user profiles"
    },
    "/song-requests/{requestId}": {
      "schema": "SongRequest",
      "description": "User song requests"
    },
    "/playlists/{playlistId}": {
      "schema": "Playlist",
      "description": "User-generated playlists"
    },
    "/admins/{adminId}": {
      "schema": "Admin",
      "description": "Admin users list"
    },
    "/trending/{trackId}": {
      "schema": "TrendingTrack",
      "description": "Global trending tracks"
    }
  }
}
